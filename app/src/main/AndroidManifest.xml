<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikasikuisterbaru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gbr_pilihanganda"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gbr_pilihanganda"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiKuisTerbaru">
        <activity android:name=".Essay"></activity>
        <activity android:name=".HasilKuis" />
        <activity android:name=".Pilgan" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>